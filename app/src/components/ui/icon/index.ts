import { IconRoot, useIconContext } from "./IconRoot";
import { IconButton } from "./IconButton";
import { IconLink } from "./IconLink";

export const Icon = {
  Root: IconRoot,
  Button: IconButton,
  Link: IconLink,
};

export { useIconContext };
